module FirArchitecture {
    in samples      : vec<4, u8>
    in coefficients : vec<4, u8>
    out y           : u18

    y = architecture(auto, parallelism<=2, depth<=2, candidates<=3) {
          samples[0] * coefficients[0]
        + samples[1] * coefficients[1]
        + samples[2] * coefficients[2]
        + samples[3] * coefficients[3]
    }
}
